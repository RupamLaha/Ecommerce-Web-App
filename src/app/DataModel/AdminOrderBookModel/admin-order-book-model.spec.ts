import { AdminOrderBookModel } from './admin-order-book-model';

describe('AdminOrderBookModel', () => {
  it('should create an instance', () => {
    expect(new AdminOrderBookModel()).toBeTruthy();
  });
});
